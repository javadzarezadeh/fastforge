APP_NAME="FastForge"
DEBUG=true
SECRET_KEY="your-secure-random-key-here"  # Generate with: python -c "import secrets; print(secrets.token_hex(32))"
ADMIN_SECRET_KEY="your-admin-secret-key-here"  # Generate with: python -c "import secrets; print(secrets.token_hex(32))"
ACCESS_TOKEN_EXPIRE_MINUTES=30
OTP_EXPIRE_MINUTES=5
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
POSTGRES_DB=fastforge
DB_PORT=5432
# Leave DATABASE_URL empty to allow dynamic configuration based on environment
# The application will automatically determine the correct host based on RUNNING_IN_DOCKER environment variable
DATABASE_URL=""
REDIS_URL="redis://redis:6379/0"
LOG_LEVEL="INFO"
ALLOWED_ORIGINS="http://localhost:3000,https://myapp.com"
ENV=development  # Set to 'production' for production
SMS_SERVICE_TYPE=mock # Options: mock, twilio, etc.
EMAIL_SERVICE_TYPE=mock # Options: mock only for now
